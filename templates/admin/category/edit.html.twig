{% extends 'base' %}
{% block body %}


    <h1>Edition Catégorie</h1>
    {#ouvre la balise form #}
    {{ form_start(form) }}

    {#Bloc de formulaire pour l'attribut name#}
    {{ form_row(form.name) }}



    <div class="text-right">
        <button type="submit" class="btn btn-primary">Enregistrer</button>
        <a href="{{ path('app_admin_category_index') }}" class="btn btn-outline-secondary">Retour</a>
    </div>



    {#ferme la balise form #}
    {{ form_end(form) }}
{% endblock %}
